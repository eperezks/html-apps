{
  "name": "html-apps",
  "private": true,
  "version": "1.0.0",
  "description": "A collection of small, single-purpose HTML apps",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1"
  }
}
